{
  "name": "@starter/dolos",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "infra:up": "docker compose -f docker-compose.yml up -d",
    "infra:down": "docker compose -f docker-compose.yml down",
    "infra:logs": "docker compose -f docker-compose.yml logs -f --tail=100"
  }
}
